# Known Issues — MPCNC_v3.0_Beta3.cps

Findings from a general code-quality review (logic errors, unhandled edge cases, dead code,
unclear variable usage). This pass did **not** check the file against Autodesk's post processor
spec — that's a separate review.

Legend: `[x]` fixed, `[ ]` open.

## Breaking bugs

- [x] **#1 — Severity: Breaking** — [Line 1981](../MPCNC_v3.0_Beta3.cps#L1981) — `toolChange()` sent `M17` (Enable Steppers) instead of `M18`/`M84` (Disable Steppers) when "Disable Z stepper" was enabled, keeping the Z axis energized during a manual tool change despite the operator prompt saying it would be disabled. **Fixed** in commit `648aec9` (changed to `M84`).
- [x] **#2 — Severity: Breaking** — [Line 822](../MPCNC_v3.0_Beta3.cps#L822) — `propertyDefinitions.mapF_SafeZ.title` referenced an undefined variable (properties are just `properties` in this file), throwing a `ReferenceError` and aborting post-processing if `mapF_SafeZ` couldn't be parsed, instead of printing the intended warning. **Fixed** by using `properties.mapF_SafeZ.title` (also added the missing space before "format error").
- [x] **#3 — Severity: Breaking** — [Line 1742](../MPCNC_v3.0_Beta3.cps#L1742) — `writeComment(...)` was called with a single argument (missing the `level` param), so it always bypassed the comment-level filter and wrote a literal `;undefined` line after every loaded custom G-code file. **Fixed** by passing `eComment.Info` as the level and the correct text as the second argument.

## Correctness risks

- [x] **#4 — Severity: Correctness** — [Line 1819](../MPCNC_v3.0_Beta3.cps#L1819) — `spindleOn` wrote `sOutput.format(spindleSpeed)` (the framework-injected global, like `tool`/`currentSection`/`unit`) instead of the function's own `_spindleSpeed` parameter. Was masked because all call sites happened to pass matching values. **Fixed** by using `_spindleSpeed`.
- [x] **#5 — Severity: Correctness** — [Lines 1397-1410](../MPCNC_v3.0_Beta3.cps#L1397-L1410) — `setSpindeSpeed` only compared speed value, ignoring `_clockwise`; a direction reversal at unchanged RPM silently emitted no G-code. **Fixed** by tracking `currentSpindleClockwise` alongside `currentSpindleSpeed` and re-triggering `spindleOn` when direction changes while running.
- [x] **#6 — Severity: Correctness** — [Line 1499](../MPCNC_v3.0_Beta3.cps#L1499) — `handleMinMax` compared `pair.max < rmin` instead of `rmax`, under-reporting the true max range in the informational Ranges Table header comment. **Fixed** by comparing against `rmax` and dropping the stale "changed by DG" comment.
- [x] **#7 — Severity: Correctness** — [Lines 1053-1086](../MPCNC_v3.0_Beta3.cps#L1053-L1086) — `onClose` only called `flushMotions()` once, before the final `rapidMovementsXY(0, 0)`/coolant-off/spindle-off/"Job end" message, so those could fire before the machine finished moving to origin (Marlin/RepRap; GRBL's `flushMotions()` is a no-op and its command queue is already sequential). Also affected the `gcodeStopFile` branch, where a custom footer file loaded via `loadFile()` could contain its own motion with no wait afterward. **Fixed** by adding `flushMotions()` after `COMMAND_STOP_SPINDLE` in the default branch, and after `loadFile()` in the custom-stop-file branch.
- [x] **#8 — Severity: Correctness** — [Line 2041](../MPCNC_v3.0_Beta3.cps#L2041) — `rapidMovementsZ(...)` was called with a stray second argument (`false`); the function only takes one parameter (`_z`), so it was silently dropped — likely a remnant of an incomplete refactor. **Fixed** by removing the unused argument.
- [x] **#9 — Severity: Correctness** — [Line 711](../MPCNC_v3.0_Beta3.cps#L711) — Typo `alue: 2` instead of `value: 2` in `eSafeZ.prop[2]`. Was unused elsewhere, but a landmine if ever referenced. **Fixed** by correcting the key name.
- [x] **#10 — Severity: Correctness** — [Lines 326-333](../MPCNC_v3.0_Beta3.cps#L326-L333) — `probe3_Thickness` declared `type: "integer"` but defaulted to `0.8` (fractional) — type/value mismatch. **Fixed** by changing the type to `"number"`.
- [x] **#19 — Severity: Correctness** — [Line 1186](../MPCNC_v3.0_Beta3.cps#L1186), [Line 1987](../MPCNC_v3.0_Beta3.cps#L1987), [`askUser`: 1930-1948](../MPCNC_v3.0_Beta3.cps#L1930-L1948), [`display_text`: 1846-1856](../MPCNC_v3.0_Beta3.cps#L1846-L1856) — `sectionComment` (from Fusion's `operation-comment` parameter) and `tool.comment` are arbitrary user-typed text passed into `display_text()`/`askUser()` and embedded directly into G-code messages (M0/M117/M291) with no sanitization. Marlin/GRBL treat `(` `)` and `;` anywhere on a line as inline-comment markers, so any of those in a tool comment or operation name would silently truncate the message; RepRap's M291 double-quotes its params, so an embedded `"` would break that quoting; and an embedded newline/CR in any destination would physically split the line, potentially leaving a stray token on its own line that a controller could misinterpret as an unintended move. `writeComment()` already strips parentheses from its own text — the same protection was never applied to `askUser`/`display_text`. **Fixed** by adding a `sanitizeMessageText(text, unsafeChars)` helper (collapses `\r`/`\n` plus a caller-supplied set of unsafe characters into a single space) and applying it at all 4 call sites: `"();"` for the GRBL/default-Marlin destinations, `"\""` for the RepRap destination. (The helper's original `.trim()` was later dropped when it was also reused by `writeComment` in #24, so that indentation-bearing comment text is preserved; the message call sites don't rely on trimming.)
- [x] **#24 — Severity: Correctness** — [`writeComment`: 1587-1599](../MPCNC_v3.0_Beta3.cps#L1587-L1599) — `writeComment()` stripped `(`/`)` but **not** `\r`/`\n`, and receives the same untrusted user text as #19 — `tool.comment` and `sectionComment` — at [1131](../MPCNC_v3.0_Beta3.cps#L1131), [1158](../MPCNC_v3.0_Beta3.cps#L1158), [1162](../MPCNC_v3.0_Beta3.cps#L1162), [1545](../MPCNC_v3.0_Beta3.cps#L1545). An embedded newline split `writeln(";" + text)` (or the GRBL `(...)` form) into two physical lines; the second fragment lost its comment prefix and became active G-code — the same failure mode as #19. (`;`/`(`/`)` are not a problem here since the whole line is already a comment; newline was the only exposure.) **Fixed** by reusing the #19 `sanitizeMessageText(text, "()")` helper in `writeComment` (collapsing parens and newlines to a space). The helper's `.trim()` was removed at the same time so comment indentation (e.g. the `   X: Min=…` ranges-table rows) is preserved.

## Dead code / style / maintainability

- [x] **#11 — Severity: Style** — [Lines 424-431](../MPCNC_v3.0_Beta3.cps#L424-L431) — `cutter3_OnEtch` property was defined twice, identically. Harmless but redundant. **Fixed** by removing the duplicate block.
- [x] **#12 — Severity: Style** — [Line 1089](../MPCNC_v3.0_Beta3.cps#L1089), [Lines 1110-1111](../MPCNC_v3.0_Beta3.cps#L1110-L1111), [Line 1450](../MPCNC_v3.0_Beta3.cps#L1450) — Implicit globals (`vectorX`, `vectorY`, `sectionComment`, `strCoolant`) from missing `var`/`let`. **Fixed**: `vectorX`/`vectorY` (in `onSection`) and `strCoolant` (in `onCommand`) are each used only within their own function, so they got a local `var` at their assignment; `sectionComment` is written in `onParameter` but read in `onSection`, so it needed a proper module-level `var sectionComment;` declaration instead.
- [x] **#13 — Severity: Style** — [Line 829](../MPCNC_v3.0_Beta3.cps#L829) — Monkey-patched `Number.prototype.round` instead of using a standalone helper function. **Fixed** by replacing it with a `roundTo(value, places)` function and updating its one call site.
- [x] **#14 — Severity: Style** — [Lines 1810-1844](../MPCNC_v3.0_Beta3.cps#L1810-L1844) — `spindleOn`/`spindleOff` used bare `this.spindleEnabled`, relying on non-strict-mode `this`-is-global semantics instead of an explicit module-level variable. **Fixed** by adding `var spindleEnabled = false;` and replacing every `this.spindleEnabled` reference with it.
- [x] **#15 — Severity: Style** — [Lines 1599-1601](../MPCNC_v3.0_Beta3.cps#L1599-L1601), [1617-1619](../MPCNC_v3.0_Beta3.cps#L1617-L1619) — Stale comments ("Rapid movements with G1", "No longer called for general Rapid only for probing, homing, etc.") that no longer matched actual behavior (emits G0; used for every general rapid via `rapidMovements()`). **Fixed** by rewriting both header comments (and the adjacent `rapidMovements()` comment at line 1634, which had the same "with G1" inaccuracy) to describe actual current behavior.
- [x] **#16 — Severity: Style** — [Line 2014](../MPCNC_v3.0_Beta3.cps#L2014), [Line 2042](../MPCNC_v3.0_Beta3.cps#L2042) — `getProperty(properties.probe7_SafeZ) != ""` was vestigial from an earlier string-typed property, and worse than just dead: `!=` coerces `""` to `0`, so it evaluated false (skipping the retract) specifically when Safe Z was set to `0`. **Fixed** by removing both guards, since the property is `type: "integer"` and always holds a numeric value.
- [x] **#17 — Severity: Style** — [Line 21](../MPCNC_v3.0_Beta3.cps#L21) — Missing semicolon after `minimumRevision` assignment, inconsistent with the rest of the file. **Fixed** by adding the semicolon.
- [x] **#18 — Severity: Style** — [Line 3](../MPCNC_v3.0_Beta3.cps#L3), [Line 13](../MPCNC_v3.0_Beta3.cps#L13) — Header comment and `description` string both said "Beta 1" while the file is `MPCNC_v3.0_Beta3.cps` (Beta 3) — stale version metadata. **Fixed** by updating both to `v4.0 (Beta 1)`, so the header and description now agree with each other (tied to the `v4.0-dev` branch).
- [x] **#20 — Severity: Style** — [Lines 1142, 1146, 1149, 1153](../MPCNC_v3.0_Beta3.cps#L1142-L1153) (`jetModeStr = "..."` assignments), [Line 1951](../MPCNC_v3.0_Beta3.cps#L1951), [Line 2005](../MPCNC_v3.0_Beta3.cps#L2005), [Line 2013](../MPCNC_v3.0_Beta3.cps#L2013) — Missing trailing semicolons beyond the one instance fixed for #17. ASI protected all of them from being an actual runtime bug (none was followed by a line starting with `(`, `[`, `` ` ``, `+`, or `-`), so this was a consistency gap, not a functional risk. **Fixed** by adding the missing semicolons.
- [x] **#21 — Severity: Style** — [`parseSafeZProperty`: 721-749](../MPCNC_v3.0_Beta3.cps#L721-L749) — `safeZHeightDefault` is declared as a Number (`var safeZHeightDefault = 15;`) but was reused to hold the raw `str.match(...)` array and then reassigned to the *string* captured by its match group (line 743), with no `Number()`/`parseFloat()` conversion. It flows into `safeZHeight` and is compared with `>=` against a real Number in `isSafeToRapid` (line 841) — this only "worked" because JS's relational operators coerce a string operand to a number when compared against a genuine Number. **Fixed** by holding the match result in its own `match` variable and wrapping `match[1]` in `Number(...)`, so `safeZHeightDefault` is always a real Number and is never reused to hold the intermediate match array.
- [x] **#22 — Severity: Style** — [`safeZforSection`: 780-818](../MPCNC_v3.0_Beta3.cps#L780-L818) — The FEED and RETRACT cases use `let feed = ...` / `let retract = ...`, but the otherwise-identical CLEARANCE case used `var clearance = ...` (line 802). Purely cosmetic inconsistency across three parallel branches. **Fixed** by changing it to `let clearance`, matching the other two branches.
- [x] **#23 — Severity: Style** — [`toolChange`: 1950-1956](../MPCNC_v3.0_Beta3.cps#L1950-L1956) — `writeComment(..., " Tool Change Start")` was written unconditionally before the `toolChange0_Enabled` check; when that property was false, the function returned immediately with no matching "Tool Change End" comment (written at line 2005) and no tool-change G-code at all. The asymmetric comment pair was cosmetic only — the no-op behavior itself is the property's intended semantics (manual tool changes handled outside the NC file). **Fixed** by moving the early-return check before the comment, so nothing is written when tool changes are disabled.
- [x] **#25 — Severity: Style** — [Line 1259](../MPCNC_v3.0_Beta3.cps#L1259) — The `circular(clockwise, cx, cy, cz, x, y, z, feed)` call statement at the end of `onCircular` was missing its trailing semicolon — the same consistency gap as #17/#20, missed in the #20 sweep (which only covered the `jetModeStr` assignments and a few `writeComment` calls). ASI protected it (next line is `}`), so no functional impact. **Fixed** by adding the semicolon.
- [x] **#26 — Severity: Style** — [Lines 1757-1771 (pre-fix)](../MPCNC_v3.0_Beta3.cps) — A commented-out block sketched an abandoned OOP firmware design (`FirmwareBase`/`Firmware3dPrinterLike` constructors + prototype wiring, `mergeProperties`), including `this.spindleEnabled = false`. It contradicted the live procedural architecture (firmware is the `fw` string vs the `eFirmware` enum — no classes anywhere), referenced `FirmwareBase` which is defined nowhere (would `ReferenceError` if uncommented), and was the origin of the fragile `this.spindleEnabled` pattern already replaced in #14. Dead, non-functional, and actively confusing. **Fixed** by deleting the block.
